package com.orientechnologies.ldbc.snb.benchmark.loader.dto;

public final class ContainerOfDTO extends RelationDTO {
  public ContainerOfDTO(long id, boolean stop, long to) {
    super(id, stop, to);
  }
}
