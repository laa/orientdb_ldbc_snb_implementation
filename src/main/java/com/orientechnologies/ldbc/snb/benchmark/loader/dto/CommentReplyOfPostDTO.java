package com.orientechnologies.ldbc.snb.benchmark.loader.dto;

public class CommentReplyOfPostDTO extends RelationDTO {
  public CommentReplyOfPostDTO(long id, boolean stop, long to) {
    super(id, stop, to);
  }
}
